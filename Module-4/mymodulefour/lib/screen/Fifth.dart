import 'package:flutter/material.dart';

class MyFifth extends StatelessWidget {
  const MyFifth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PROGRAM - 5")),
      body: Center(
        child: Text("PROGRAM - 5"),
      ),
    );
  }
}