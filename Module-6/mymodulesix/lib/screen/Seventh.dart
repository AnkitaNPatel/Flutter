import 'package:flutter/material.dart';

class MySeventh extends StatelessWidget {
  const MySeventh({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PROGRAM - 7")),
      body: Center(
        child: Text("PROGRAM - 7"),
      ),
    );
  }
}