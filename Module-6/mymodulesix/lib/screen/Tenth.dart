import 'package:flutter/material.dart';

class MyTenth extends StatelessWidget {
  const MyTenth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PROGRAM - 10")),
      body: Center(
        child: Text("PROGRAM - 10"),
      ),
    );
  }
}