import 'package:flutter/material.dart';

class MySixth extends StatelessWidget {
  const MySixth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PROGRAM - 6")),
      body: Center(
        child: Text("PROGRAM - 6"),
      ),
    );
  }
}