import 'package:flutter/material.dart';

class MyNinth extends StatelessWidget {
  const MyNinth({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PROGRAM - 9")),
      body: Center(
        child: Text("PROGRAM - 9"),
      ),
    );
  }
}